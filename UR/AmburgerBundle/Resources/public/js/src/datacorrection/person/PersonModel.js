
PersonCorrection.PersonModel = (function(){
    var that = {},
    
    /* 
        Initialises the object and sets default values.
    */
    init = function() {

        return that;
    },

    /* 
        Sets the person from a given json string.
    */
    createFromJson = function(json){
        console.log(json);
    };


    that.init = init;
    that.createFromJson = createFromJson;

    return that;
})();

